/* -----------------------------------------------------TESTING BITS OF CODE-------------------- */


function calc(x, y) {

    const quotient = y/x;
    const remainder = y % x;
    console.log(Math.floor(quotient));
    console.log(remainder);
}
let x = 20;
let y= 46.75;

calc(x, y);
